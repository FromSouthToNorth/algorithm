package vip.hyzt.questions;

import org.junit.Test;

public class Topic788RotatedDigitsTests {

    @Test
    public void rotatedDigits() {
        Topic788RotatedDigits topic788RotatedDigits = new Topic788RotatedDigits();
        System.out.printf("res %d\n", topic788RotatedDigits.rotatedDigits(10));
        System.out.printf("res %d\n", topic788RotatedDigits.rotatedDigits(10000));
    }

}
